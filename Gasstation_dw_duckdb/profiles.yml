Gasstation_dw_duckdb:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: dev.duckdb
      threads: 1
      
name: 'Gasstation_dw_duckdb'
version: '1.0.0'
config-version: 2

profile: 'Gasstation_dw_duckdb'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  Gasstation_dw_duckdb:
    staging:
      +materialized: view
    datawarehouse:
      +materialized: table
